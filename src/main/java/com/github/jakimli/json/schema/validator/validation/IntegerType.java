package com.github.jakimli.json.schema.validator.validation;

class IntegerType extends Type {

    IntegerType(Schema schema) {
        super(schema);
    }

    @Override
    protected void configure() {
    }
}
