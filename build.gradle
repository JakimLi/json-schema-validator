apply plugin: 'java'

ext {
    junit = '4.12'
    fastjson = '1.2.47'
}

repositories {
    mavenCentral()
}

dependencies {
    compile(
            "junit:junit:${junit}",
            "com.alibaba:fastjson:${fastjson}"
    )
}

wrapper {
    gradleVersion = '4.10.2'
}
